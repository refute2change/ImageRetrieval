# Instructions on running this project

1. Clone the project.
2. Install the necessary modules `pip install streamlit sentence-transformers scikit-learn pillow numpy`
3. Run via `streamlit run app.py`
